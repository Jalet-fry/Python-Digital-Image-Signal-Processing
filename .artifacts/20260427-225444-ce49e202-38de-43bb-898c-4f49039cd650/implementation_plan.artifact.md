# План завершения Лабораторной работы №4 (AI Audio)

Цель: Интегрировать результаты из Google Colab (обученные веса и логи) в локальный проект, обеспечить работу GUI на основе реальных данных и подготовить доказательства для защиты (пункты 2.1 - 2.4 методички).

## Предложенные изменения

### 1. Интеграция обученных моделей (Пункт 2.1)
- Использовать `vocoder_finetuned_actual.pth` и `vocoder_finetuned_miner.pth` как результат дообучения вокодера.
- Обновить `VoiceProcessor`, чтобы он мог загружать эти веса.

#### [voice_processor.py](file:///C:/Univer/BSUIR_LABS/6_term/ЦОСиИ/PythonDSP/core/dsp/voice_processor.py)
- Добавить метод `load_custom_vocoder(weights_path)`.
- Интегрировать использование загруженных весов в `run_neural_tts`.

### 2. Реализация GUI для новых данных
- Обновить `Lab4AI` в `labs/lab4_ai.py`, чтобы кнопки "LOGS" и "STABILITY" использовали данные, полученные в ходе реальных экспериментов.

#### [lab4_ai.py](file:///C:/Univer/BSUIR_LABS/6_term/ЦОСиИ/PythonDSP/labs/lab4_ai.py)
- Настроить считывание `hifigan_train_log.csv` (реальные логи из Colab).
- Обеспечить проигрывание `result_finetuned.wav` как примера "до/после".

### 3. Автоматизация ресурсов (Пункт 2.2)
- Добавить в `run_vc` и `run_tts` автоматический замер ресурсов через `psutil` (уже частично есть, проверить точность).

---

## План выполнения (Конкретные шаги)

1. **Шаг 1: Подготовка данных из Colab.**
   - Поместить `vocoder_finetuned_actual.pth` в `core/models/` (создать папку).
   - Создать `hifigan_train_log.csv` на основе данных из Colab (если его еще нет локально).

2. **Шаг 2: Модификация `voice_processor.py`.**
   - Добавить поддержку загрузки кастомных весов `.pth` в модель `SpeechT5HifiGan`.

3. **Шаг 3: Синхронизация `lab4_ready`.**
   - Обновить файлы в `lab4_ready`, чтобы они соответствовали последним правкам в `core/dsp` и `labs`.

4. **Шаг 4: Проверка и фиксация ресурсов.**
   - Запустить `main.py` -> Лабораторная 4.
   - Прогнать VC и TTS.
   - Снять скриншоты нагрузки через PowerShell (`Get-Process`).

## План верификации

### Автоматизированные тесты
- Запуск `python test_lab4_logic.py` (обновить его для работы с загруженными весами).

### Ручная проверка
- Запуск GUI через `python labs/lab4_ai.py`.
- Нажатие кнопки `LOGS (2.1)` -> проверка отрисовки реального графика Loss.
- Нажатие кнопки `STAB. (2.3)` -> проверка графика косинусного сходства.
- Прослушивание `results/lab4_vc.wav` (голос должен быть чище благодаря дообученному вокодеру).
